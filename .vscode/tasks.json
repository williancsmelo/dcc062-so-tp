{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile",
      "type": "shell",
      "windows": {
        "command": [
          "rm build -r -ErrorAction:SilentlyContinue;",
          "mkdir build;",
          "cd build;",
          "gcc -c ../*.c;",
          " gcc -o ../main.exe *.o;",
          "../main.exe"
        ]
      },
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
